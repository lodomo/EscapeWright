class Broadcasts():
    """
    These are not all inclusive, but they are common across all the rooms.
    """
    ROOM_START = "ROOM_START"
    START = "START"
    PAUSE = "PAUSE"
    RESUME = "RESUME"
    STOP = "STOP"
    RESET = "RESET"


class Statuses():
    INIT = "INIT"
    READY = "READY"
    ACTIVE = "ACTIVE"
    STOPPED = "STOPPED"
    RESET = "RESET"
    PAUSED = "PAUSED"
